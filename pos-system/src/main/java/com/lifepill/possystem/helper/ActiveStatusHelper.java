/*
package com.lifepill.possystem.helper;

import com.lifepill.possystem.repo.employerRepository.EmployerRepository;
import lombok.*;
import org.springframework.beans.factory.annotation.Autowired;

@Setter
@Getter
@ToString
@AllArgsConstructor
@RequiredArgsConstructor
public class ActiveStatusHelper {

    final EmployerRepository employerRepository;

    private void updateActiveStatus(String username) {
        // Implement your logic to update the active status in the database
        // For example, you can call a method in the repository to update the active status for the given username
        employerRepository.updateActiveStatusByEmail(username, true);
    }
}
*/
