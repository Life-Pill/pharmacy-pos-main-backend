/*
package com.lifepill.possystem.util;

import javax.servlet.http.HttpSession;

public class AuthUtils {

    // Method to check if the user is logged in based on the session
    public static boolean isLoggedIn(String request) {
        // Get the session from the request, create a new session if it doesn't exist
        HttpSession session = request.getSession(false);

        // Check if the session exists and contains an attribute indicating the user is logged in
        if (session != null && session.getAttribute("loggedInUser") != null) {
            return true; // User is logged in
        } else {
            return false; // User is not logged in
        }
    }
}
*/
